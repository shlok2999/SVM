version: '3.9'
services:
  fjasldf:
    container_name: fjasldf
    build:
      context: .
    ports:
    - 9000:9000/tcp
    - 9080:980/udp
    tty: true
    volumes:
    - type: tmpfs
      target: /target
      tmpfs:
        size: 1G
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
        reservations:
          devices:
          - driver: nvidia
            count: 1
            capabilities:
            - gpu
